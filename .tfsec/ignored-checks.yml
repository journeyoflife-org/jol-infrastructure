# tfsec ignored checks
# Each override MUST include a business justification
# Reviewed quarterly as part of SOC 2 audit

exclusions:
  # Trivy IaC inline suppressions (#trivy:ignore:) in terraform/modules/ —
  # pre-existing intentional design, first surfaced in CI once trivy-action
  # was pinned to a resolvable tag (PR #27).
  - rule: trivy AWS-0040/AWS-0041
    justification: "Prod EKS endpoint is private-only; dev/staging public API access gated by IAM + aws-auth RBAC (ADR-002)"
    expires: "2026-11-30"
  - rule: trivy AWS-0104
    justification: "EKS control-plane egress is AWS-managed traffic; restricting breaks node registration"
    expires: "2026-11-30"
  - rule: trivy AWS-0164
    justification: "Public tier hosts only NAT gateways + internet-facing ALBs per 3-tier segmentation (ADR-004)"
    expires: "2026-11-30"
