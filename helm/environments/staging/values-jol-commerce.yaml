# staging values for jol-commerce
replicaCount: 2

image:
  tag: "latest"  # Override in CI with git SHA

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 1024Mi

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 75

ingress:
  hosts:
    - host: "jol-commerce-staging.journeyoflife.org"
      paths:
        - path: /
          pathType: Prefix

externalSecrets:
  enabled: true
  secrets:
    - key: database-url
      remoteKey: "staging/jol-commerce/database"
      property: url
    - key: secret-key
      remoteKey: "staging/jol-commerce/app"
      property: secret_key
